Step-1
Open cmd and type 
pip install nltk
pip install numpy

Step-2
after installing both, Open CMD and type python and then
Type the below code:

import nltk
nltk.download('punkt')

Step-3
Lets run the script through CMD, as we don't have a GUI, it will look decent in CMD
Open CMD inside Duplicate_Vendor folder and type:

python DuplicateVendor_FinalVersion.py

Output will be saved inside the 'text' folder of Duplicate_Vendor Folder